__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

verifier/target/
web/node_modules/
web/dist/

corpus/*.ccert
!corpus/.gitkeep
*.log
tmp/

# Installed beside the project so a frozen system drive cannot lose it
vendor/

# Assembled releases
release/

# Published copy of the corpus; regenerate with tools\web_data.bat
web/public/data/
web/src/corpus.generated.js
desktop.ini

# Session handoff / context docs — not part of the published tree
docs/PROJECT_CONTEXT.md
docs/SPEC_v0.3.0.md
docs/удалить перед публикацией/

# One-shot maintenance script; run once then delete
normalize_eol.py

# cargo-fuzz build output and derived corpus (seeded from spec/vectors in CI)
verifier/fuzz/target/
verifier/fuzz/corpus/
verifier/fuzz/artifacts/
verifier/fuzz/coverage/
verifier/fuzz/Cargo.lock

# minisign secret keys — never in the tree. The public key lives in the
# README; the secret one stays on the signer's machine only.
*.key
minisign.key
